<!DOCTYPE html>
<html>
  <head>
    <title>AlphaBlog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render 'layouts/messages' %>  
    <!-- Reads the partial _messages.html.erb, ruby doesnt need the _ in the name to recognize it. 
         Also, having the _ in the name is common convention for partials-->
    <%= yield %>
  </body>
</html>
